<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client Action to trigger the notification system -->
    <record id="action_session_notification" model="ir.actions.client">
        <field name="name">Sessiya Bildirişləri</field>
        <field name="tag">session_notification</field>
        <field name="target">main</field>
    </record>
    
    <!-- Menu item for the notification system -->
    <menuitem id="menu_session_notification" name="Sessiya Bildirişləri" 
              parent="menu_badminton_sessions" action="action_session_notification" sequence="3"/>
    
    <!-- No scheduled action - we'll handle this directly in JS with counters -->
</odoo>
